{
  "name": "@spacemangaming/daemon",
  "author": "Spacemandev",
  "version": "0.1.7",
  "type": "module",
  "devDependencies": {
    "@rolldown/plugin-node-polyfills": "^1.0.0",
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.1",
    "rolldown": "^1.0.0-beta.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.33.1",
    "@modelcontextprotocol/sdk": "^1.1.1",
    "@solana/web3.js": "^1.98.0",
    "eventsource": "^3.0.2",
    "openai": "^4.78.1",
    "tweetnacl": "^1.0.3"
  },
  "scripts": {
    "build": "bun build --target browser --outdir dist src/index.ts",
    "build:types": "tsc --emitDeclarationOnly --declaration --outDir dist",
    "build:all": "bun run build && bun run build:types",
    "publish": "bun run build:all && bunx npm publish --access=public"
  },
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts"
}
